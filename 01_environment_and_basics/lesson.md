# 第01节：环境搭建与基础类型

> **本节目标**：理解 Go 的包管理机制，掌握基础数据类型，学会变量声明的多种方式

---

## 📖 一、核心概念讲解

### 1.1 Go 程序的基本结构

#### **对比 Java 程序结构**

```java
// Java 程序结构
package com.example.demo;           // 包声明（域名反转）

import java.util.List;              // 导入语句

public class HelloWorld {           // 类定义（必须有）
    public static void main(String[] args) {  // 主方法
        System.out.println("Hello, World!");
    }
}
```

```go
// Go 程序结构
package main                        // 包声明（简短名称）

import "fmt"                        // 导入语句

func main() {                       // 主函数（不在类中）
    fmt.Println("Hello, World!")
}
```

#### **关键差异**：

1. **无类强制**：Go 的函数可以直接定义在包级别，无需类包裹（Java 所有代码必须在类中）
2. **包名简洁**：Go 包名通常是简短单词，不需要域名反转（Java 用 `com.company.project`）
3. **大小写决定可见性**：Go 中首字母大写表示公开（public），小写表示私有（Java 用 `public/private` 关键字）
4. **main 包特殊性**：只有 `package main` + `func main()` 才能编译为可执行文件

---

### 1.2 包管理：go mod vs Maven/Gradle

#### **Java 的包管理**

```xml
<!-- Maven: pom.xml -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-web</artifactId>
    <version>2.7.0</version>
</dependency>
```

```groovy
// Gradle: build.gradle
implementation 'org.springframework.boot:spring-boot-starter-web:2.7.0'
```

#### **Go 的包管理**

```bash
# 初始化模块（只需一次）
go mod init golang_study

# 添加依赖（自动记录到 go.mod）
go get github.com/gin-gonic/gin@v1.9.0

# 整理依赖（类似 Maven 的 dependency:tree）
go mod tidy
```

```go
// go.mod 文件（类似 pom.xml）
module golang_study

go 1.24

require (
    github.com/gin-gonic/gin v1.9.0
)
```

#### **关键差异**：

| 特性         | Go (go mod)                             | Java (Maven/Gradle)              |
| ------------ | --------------------------------------- | -------------------------------- |
| **配置文件** | go.mod（简洁文本）                      | pom.xml（冗长XML）/ build.gradle |
| **依赖下载** | 自动下载到 `$GOPATH/pkg/mod`            | 下载到 `~/.m2/repository`        |
| **版本管理** | 语义化版本（v1.2.3）                    | 同样支持，但 Maven 更复杂        |
| **依赖添加** | `go get` 或直接 import 后 `go mod tidy` | 必须手动编辑 pom.xml             |
| **私有仓库** | GOPROXY 环境变量                        | settings.xml 配置                |

---

### 1.3 基础数据类型对比

#### **整数类型**

| Go 类型 | Java 对应  | 说明                                       |
| ------- | ---------- | ------------------------------------------ |
| `int`   | 无直接对应 | **平台相关**：32位系统是int32，64位是int64 |
| `int8`  | `byte`     | -128 到 127                                |
| `int16` | `short`    | -32768 到 32767                            |
| `int32` | `int`      | -2^31 到 2^31-1                            |
| `int64` | `long`     | -2^63 到 2^63-1                            |
| `uint`  | 无         | 无符号整数（Go 特有）                      |
| `uint8` | 无         | 0 到 255（byte 的别名）                    |
| `byte`  | `byte`     | uint8 的别名                               |
| `rune`  | `char`     | int32 的别名，表示 Unicode 码点            |

**关键点**：

- Java 的 `int` 固定是 32 位，但 Go 的 `int` 是平台相关的（推荐用 `int`，除非有特殊需求）
- Go 有无符号整数（`uint`），Java 没有
- Go 的 `rune` 可以表示任何 Unicode 字符（Java 的 `char` 只能表示 BMP 字符）

#### **浮点数类型**

| Go 类型   | Java 对应 | 说明                           |
| --------- | --------- | ------------------------------ |
| `float32` | `float`   | IEEE-754 32位浮点数            |
| `float64` | `double`  | IEEE-754 64位浮点数（Go 默认） |

#### **布尔类型**

| Go 类型 | Java 对应 | 差异                                               |
| ------- | --------- | -------------------------------------------------- |
| `bool`  | `boolean` | Go 中 **不能用数字代替**（Java 也不行，但 C 可以） |

**重要**：Go 中 `if 1 { }` 会编译错误，必须 `if true { }`

#### **字符串类型**

| Go 类型  | Java 对应 | 关键差异                                           |
| -------- | --------- | -------------------------------------------------- |
| `string` | `String`  | Go 的 string 是 **不可变的字节序列**，底层是 UTF-8 |

```go
// Go: 字符串是不可变的
s := "hello"
// s[0] = 'H'  // 编译错误！不能修改

// 必须转换为切片才能修改
bytes := []byte(s)
bytes[0] = 'H'
s = string(bytes)  // "Hello"
```

```java
// Java: String 也是不可变的
String s = "hello";
// s.charAt(0) = 'H';  // 编译错误

// 必须用 StringBuilder
StringBuilder sb = new StringBuilder(s);
sb.setCharAt(0, 'H');
s = sb.toString();  // "Hello"
```

---

### 1.4 变量声明方式

#### **Java 的变量声明（只有一种方式）**

```java
int age = 25;                    // 必须指定类型
String name = "张三";
List<String> list = new ArrayList<>();  // 必须显式类型
```

#### **Go 的变量声明（四种方式）**

```go
// 方式1：标准声明（类似 Java）
var age int = 25
var name string = "张三"

// 方式2：类型推断（Go 编译器自动推断）
var age = 25        // 推断为 int
var name = "张三"   // 推断为 string

// 方式3：短变量声明（最常用，只能在函数内）
age := 25           // := 表示声明并赋值
name := "张三"

// 方式4：批量声明
var (
    age   int    = 25
    name  string = "张三"
    score float64 = 98.5
)
```

#### **关键规则**：

1. **`:=` 只能用于函数内部**（不能用于包级别变量）
2. **`:=` 左侧至少有一个新变量**（否则用 `=`）
3. **未使用的变量会导致编译错误**（Java 只是警告，Go 是错误）

```go
func example() {
    age := 25       // OK：首次声明
    age = 30        // OK：赋值
    // age := 35    // 错误：age 已声明

    age, name := 30, "李四"  // OK：name 是新变量
}
```

---

### 1.5 常量与枚举

#### **Java 的常量**

```java
public class Config {
    public static final int MAX_SIZE = 100;
    public static final String APP_NAME = "MyApp";
}

// Java 枚举
public enum Status {
    PENDING(0, "待处理"),
    RUNNING(1, "进行中"),
    FINISHED(2, "已完成");

    private final int code;
    private final String desc;

    Status(int code, String desc) {
        this.code = code;
        this.desc = desc;
    }
}
```

#### **Go 的常量**

```go
// 常量声明（编译时确定，不可修改）
const MaxSize = 100
const AppName = "MyApp"

// 批量声明
const (
    MaxSize = 100
    AppName = "MyApp"
    Pi      = 3.14159
)
```

#### **Go 的 iota（枚举生成器）**

**Go 没有 enum 关键字**，但用 `iota` + `const` 实现类似功能：

```go
// iota: 常量计数器，从 0 开始，每行自增 1
type Status int

const (
    Pending  Status = iota  // 0
    Running                 // 1（自动继承 iota）
    Finished                // 2
)

// 跳过某些值
const (
    _ = iota  // 0（用 _ 丢弃）
    KB = 1 << (10 * iota)  // 1 << 10 = 1024
    MB                      // 1 << 20 = 1048576
    GB                      // 1 << 30 = 1073741824
)

// 自定义起始值
const (
    StatusPending  = iota + 1  // 1
    StatusRunning              // 2
    StatusFinished             // 3
)
```

#### **对比总结**：

| 特性           | Go (iota)        | Java (enum)          |
| -------------- | ---------------- | -------------------- |
| **类型安全**   | 弱（本质是 int） | 强（真正的枚举类型） |
| **方法绑定**   | 需要手动实现     | 支持方法和字段       |
| **简洁性**     | 极简（一行代码） | 冗长（需要构造器）   |
| **字符串转换** | 需要手动实现     | 内置 `name()` 方法   |

---

### 1.6 零值概念（Go 的特殊设计）

**Java**：未初始化的局部变量编译错误，成员变量有默认值

```java
public class Example {
    int age;     // 默认 0
    String name; // 默认 null

    public void test() {
        int x;
        // System.out.println(x);  // 编译错误：未初始化
    }
}
```

**Go**：所有变量都有零值，不存在"未初始化"

```go
var age int        // 0
var name string    // ""（空字符串，不是 nil）
var isOk bool      // false
var ptr *int       // nil
var slice []int    // nil
var m map[string]int  // nil
```

**关键点**：

- Go 的零值是**可用的**，例如空字符串可以直接操作，不会抛出 NPE
- `nil` 只用于指针、切片、map、channel、接口、函数类型

---

## 💻 二、代码示例

### 示例1：基础类型演示

```go
package main

import "fmt"

func main() {
    // ========== 整数类型 ==========
    var age int = 25                  // 显式声明
    height := 175                     // 短变量声明（类型推断为 int）
    var weight int32 = 65             // 指定 int32

    fmt.Printf("年龄: %d, 类型: %T\n", age, age)        // %T 打印类型
    fmt.Printf("身高: %d, 类型: %T\n", height, height)
    fmt.Printf("体重: %d, 类型: %T\n", weight, weight)

    // 注意：Go 中不同整数类型不能直接运算（Java 可以自动转换）
    // result := age + weight  // 编译错误：int 和 int32 不兼容
    result := age + int(weight)  // 必须显式转换
    fmt.Println("总和:", result)

    // ========== 浮点数 ==========
    var score float64 = 98.5
    price := 19.99                    // 默认 float64

    fmt.Printf("分数: %.2f, 类型: %T\n", score, score)  // %.2f 保留2位小数

    // ========== 布尔类型 ==========
    var isStudent bool = true
    hasCar := false

    fmt.Println("是否学生:", isStudent)
    fmt.Println("是否有车:", hasCar)

    // Go 中不能用数字代替布尔值
    // if 1 { }  // 编译错误
    if isStudent {  // 必须是 bool 类型
        fmt.Println("享受学生优惠")
    }

    // ========== 字符串 ==========
    var name string = "张三"
    greeting := "Hello, Go!"

    fmt.Println("姓名:", name)
    fmt.Println("问候:", greeting)

    // 字符串拼接（Go 用 +，Java 也是）
    fullName := "姓名: " + name
    fmt.Println(fullName)

    // 字符串长度（字节长度，不是字符数）
    fmt.Println("字节长度:", len(greeting))       // 10（1个英文=1字节）
    fmt.Println("字节长度:", len(name))           // 6（1个中文=3字节 UTF-8）

    // 获取字符（注意：返回的是字节）
    fmt.Printf("第一个字节: %c, 类型: %T\n", greeting[0], greeting[0])  // 'H', uint8

    // ========== 字符类型（rune）==========
    var ch rune = '中'               // 单引号表示字符（rune 本质是 int32）
    fmt.Printf("字符: %c, Unicode码点: %U, 类型: %T\n", ch, ch, ch)

    // ========== 零值演示 ==========
    var defaultInt int
    var defaultStr string
    var defaultBool bool

    fmt.Println("int 零值:", defaultInt)        // 0
    fmt.Println("string 零值:", defaultStr)     // ""（空字符串）
    fmt.Println("bool 零值:", defaultBool)      // false
    fmt.Printf("string 是否为空: %v\n", defaultStr == "")  // true
}
```

### 示例2：常量与 iota

```go
package main

import "fmt"

// ========== 常量定义 ==========
const Pi = 3.14159
const AppName = "GoLearn"

// 批量常量
const (
    MaxConnections = 100
    Timeout        = 30
)

// ========== iota 枚举 ==========
type Weekday int

const (
    Sunday    Weekday = iota  // 0
    Monday                    // 1
    Tuesday                   // 2
    Wednesday                 // 3
    Thursday                  // 4
    Friday                    // 5
    Saturday                  // 6
)

// HTTP 状态码（跳过某些值）
const (
    StatusOK       = 200
    StatusCreated  = 201
    StatusAccepted = 202
)

// 文件权限（位运算）
const (
    ReadPermission   = 1 << iota  // 1 (二进制: 001)
    WritePermission                // 2 (二进制: 010)
    ExecutePermission              // 4 (二进制: 100)
)

// 存储单位
const (
    _  = iota  // 跳过 0
    KB = 1 << (10 * iota)  // 1024
    MB                      // 1048576
    GB                      // 1073741824
    TB                      // 1099511627776
)

func main() {
    // ========== 使用常量 ==========
    fmt.Println("圆周率:", Pi)
    fmt.Println("应用名称:", AppName)

    // ========== 使用枚举 ==========
    today := Monday
    fmt.Printf("今天是星期: %d\n", today)  // 1

    if today == Monday {
        fmt.Println("今天是星期一")
    }

    // ========== 文件权限组合（位运算）==========
    // Java 中需要用 EnumSet，Go 直接用位运算
    permission := ReadPermission | WritePermission  // 按位或
    fmt.Printf("权限值: %d (二进制: %b)\n", permission, permission)

    // 检查权限（按位与）
    if permission&ReadPermission != 0 {
        fmt.Println("有读权限")
    }
    if permission&ExecutePermission != 0 {
        fmt.Println("有执行权限")
    } else {
        fmt.Println("无执行权限")
    }

    // ========== 存储单位 ==========
    fileSize := 2 * GB
    fmt.Printf("文件大小: %d 字节 (%.2f GB)\n", fileSize, float64(fileSize)/float64(GB))
}
```

---

## 🎯 三、随堂练习

### 练习要求：

编写一个 Go 程序 `exercise.go`，实现以下功能：

1. **定义个人信息变量**：
   - 姓名（string）
   - 年龄（int）
   - 身高（float64，单位：米）
   - 是否在职（bool）

2. **定义常量**：
   - 公司名称（string）
   - 最低年龄要求（int）= 18

3. **定义员工状态枚举**（使用 iota）：
   - 试用期（Probation）= 1
   - 正式员工（Regular）= 2
   - 已离职（Resigned）= 3

4. **实现功能**：
   - 打印所有个人信息
   - 判断年龄是否满足入职要求（>= 最低年龄）
   - 根据员工状态输出不同提示信息

### 提示：

- 使用短变量声明（`:=`）定义变量
- 使用 `fmt.Printf` 格式化输出
- 使用 `if-else` 进行条件判断

### 期望输出示例：

```
===== 个人信息 =====
姓名: 李四
年龄: 25
身高: 1.75 米
是否在职: true
公司: 阿里巴巴

===== 年龄检查 =====
年龄符合入职要求（>= 18岁）

===== 员工状态 =====
当前状态: 2
状态说明: 正式员工
```

---

## 📝 提交方式

完成后，将代码保存为 `e:\Golang_study\01_environment_and_basics\exercise.go`，然后在终端运行：

```bash
cd e:\Golang_study\01_environment_and_basics
go run exercise.go
```

把你的代码和运行结果发给我，我会进行 Code Review！ 🔍

---

## 🔑 关键知识点总结

| 知识点       | Go 特点              | vs Java               |
| ------------ | -------------------- | --------------------- |
| **包管理**   | go mod（简洁）       | Maven/Gradle（复杂）  |
| **变量声明** | 4种方式，`:=` 最常用 | 只有1种               |
| **类型推断** | 支持                 | Java 10+ 才有 `var`   |
| **零值**     | 所有变量都有零值     | 局部变量需初始化      |
| **类型转换** | 必须显式转换         | 小类型可自动转大类型  |
| **枚举**     | iota + const（轻量） | enum（重量级）        |
| **字符串**   | 不可变，UTF-8        | 不可变，UTF-16        |
| **可见性**   | 大小写控制           | public/private 关键字 |

**下一节预告**：我们将学习 Go 的函数与方法，包括多返回值、defer、错误处理等特性！
